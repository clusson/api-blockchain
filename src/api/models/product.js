class Product {
  constructor(product) {
    this.productIdentifier = product.productIdentifier
    this.productName = product.productName
    this.productCode = product.productCode
    this.scanDate = product.scanDate
    this.ScanId = product.scanId
  }
}
export default Product
